# ── 历史版本快照（只读 latest，不读历史版本）──────────────────────────
# 04_optimize/{机制}/v*.* 目录是归档记录，执行时只通过 latest 软链接访问
04_optimize/*/v*/

# ── 中间产物（执行时按需显式读取，不自动加载）────────────────────────
# 学习报告、机制解析：每次执行 Step 1 时按需读取
01_learn/
# diff 校验报告：Step 3 Compare 时按需读取
03_compare/

# ── 验证凭证（仅人工查阅，不参与执行）────────────────────────────────
product/sample/

# ── 生成代码目录（仅占位，代码直接写目标工程）─────────────────────────
02_generate/output/
