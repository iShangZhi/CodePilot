# ── 本地路径配置（含敏感路径，不入库）──────────────────────────────────
.claude/config.yml
.claude/settings.local.json

# ── SKILL 历史版本（只保留 latest 软链接和状态文件）──────────────────
product/skills/*/v*/

# ── macOS ────────────────────────────────────────────────────────────
.DS_Store
