# 快捷指令

| 指令 | 作用 |
|------|------|
| `启动 CodePilot` | 读取配置 → 判断模式 → 执行完整流程 |
| `继续` | 本轮结束后继续下一轮迭代 |
| `发布 Skill` | 触发 `/publish` 指令，同步至 product/skill/ 和 skill_library |
| `/publish` | 直接执行发布（同上，可单独调用） |
